#include <iostream>
#include <vector>
using namespace std;
void nextPermutation(vector<int> &nums)
{
  /*
  // Next Permutation Algorithm

// Step 1: Find the pivot index.
// Traverse from right to left and find the first index i such that
// nums[i] < nums[i + 1].

// Step 2: If no such index exists (i becomes -1),
// reverse the entire array and return.
// (The array is in descending order and is the last permutation.)

// Step 3: Find index j.
// Traverse from the end of the array and find the first element
// nums[j] > nums[i] (greater than the pivot).

// Step 4: Swap nums[i] and nums[j].

// Step 5: Reverse the subarray from (i + 1) to (n - 1).
// This makes the suffix as small as possible.

// Result: You get the next lexicographically greater permutation.
  
  */
}